{
  "name": "comments-api",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Add your build command here'",
    "test": "echo 'Add your test command here'",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.0.0"
  }
} 